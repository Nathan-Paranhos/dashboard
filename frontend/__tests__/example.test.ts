// Teste de exemplo para verificar se o Jest está configurado corretamente
describe('Exemplo de teste', () => {
  it('deve passar quando 1 + 1 for igual a 2', () => {
    expect(1 + 1).toBe(2);
  });
});
